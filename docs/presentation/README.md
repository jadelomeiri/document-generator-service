# Presentation assets

The previous generated presentation files were specific to the old repository domain and have been removed.

For now, use `docs/PRESENTATION_NOTES.md` as the lightweight Document Generator interview walkthrough. A generated slide deck can be added later after the Java implementation has been migrated and the final API shape is known.
